diff --git a/src/models/models/modeling_qwen3_vl_semvid.py b/src/models/models/modeling_qwen3_vl_semvid.py
--- a/src/models/models/modeling_qwen3_vl_semvid.py
+++ b/src/models/models/modeling_qwen3_vl_semvid.py
@@ -1768,6 +1768,7 @@ class Qwen3VLForConditionalGenerationSemVID(Qwen3VLPreTrainedModel, GenerationMi
                     query_embed=query_embed,
                     device=device,
                     return_coords=True,
+                    merged_grid_hw=self._merged_grid_for_sample(video_grid_thw, b, F, P),
                 )
                 if semantic_prune_coords_batch is not None and keep_coords is not None:
                     semantic_prune_coords_batch["context"].append(keep_coords["context"])
@@ -2397,5 +2398,6 @@ class Qwen3VLForConditionalGenerationSemVID(Qwen3VLPreTrainedModel, GenerationMi
             query_embed: torch.FloatTensor,  # (D,)
             device: torch.device,
             return_coords: bool = False,
+            merged_grid_hw: Optional[tuple[int, int]] = None,
     ):
         """Semantic-oriented pruning for a *single* video's token region.
@@ -2817,6 +2819,16 @@ class Qwen3VLForConditionalGenerationSemVID(Qwen3VLPreTrainedModel, GenerationMi
         }
         return pruned_video_tokens, keep_in_video_region, keep_coords
-
+
+    def _merged_grid_for_sample(
+            self,
+            video_grid_thw: Optional[torch.LongTensor],
+            batch_index: int,
+            frame_count: int,
+            patches_per_frame: int,
+    ) -> Optional[tuple[int, int]]:
+        """Selection-hook metadata extension; official SemVID does not consume it."""
+        return None
+
     def _fastvid_select_keep_indices_with_budget(
             self,
             video_hidden_states: torch.Tensor,  # (T, P, D)
